// Import all component modules
import { enableFormValidation } from './component/form.js';

// Initialize everything
document.addEventListener("DOMContentLoaded", () => {
    // initCards();
    enableFormValidation();
    console.log("App initialized");
});

